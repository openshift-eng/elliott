name: test-source

# Distgit branch to use with this group
branch: rhaos-3.3-rhel-7

# Sources contains a mapping of source alias (found in config yaml) to
# github sources.
sources:
  # An alias which can be used to reference this source in the config yamls of this group.
  openshift-ansible:
    url: https://github.com/openshift/openshift-ansible
    branch:
      target: release-1.3
      fallback: master
